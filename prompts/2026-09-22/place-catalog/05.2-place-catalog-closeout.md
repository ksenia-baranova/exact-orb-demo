# Промт 5.2 — полная проверка и documentation closeout каталога мест

## Что делаем и зачем

Завершить ветку `feat/place-catalog`: проверить все принятые карточки на полном
наборе тестов и реальных локальных GeoNames-данных, затем привести актуальные
requirements, scenarios, diagrams, overview и roadmap к фактическому статусу.

Документы должны ясно разделять реализованный M1-5 catalog core и следующие
этапы: HTTP mapping/lifespan остаются M1-6, browser autocomplete — M1-7,
доставка производного `places.sqlite` — M1-12.

Production-код и тесты в этой карточке не меняются. Если проверки обнаруживают
дефект, зафиксировать точный отказ и вынести исправление в отдельную узкую
карточку, не скрывая его документационной правкой.

## Источники истины и готовность

- [AGENTS.md](../../../AGENTS.md).
- [План каталога мест](../../../docs/project_management/implementation_plans/place_catalog_implementation_plan.md):
  карточка 5.2, матрица AC и Definition of Done.
- [Требования каталога](../../../docs/requirements/component_responsibilities/exact-orb_place_catalog.md):
  AC-P1–P11, AC-S1–S14, AC-L1–L7 и AC-B1–B7.
- [Требования резолва](../../../docs/requirements/component_responsibilities/exact-orb_birth_data_resolution.md),
  [overview](../../../docs/requirements/overview.md),
  [scenarios](../../../docs/requirements/scenarios.md),
  [sequence diagrams](../../../docs/sequence_diagrams/place_catalog/README.md)
  и [roadmap](../../../docs/project_management/roadmap.md).
- Текущая реализация, тесты и журналы карточек 1.1–5.1.

Перед изменениями подтвердить ветку `feat/place-catalog`, HEAD
`53b872a39ecde0afd9a71fc8168158438b4bf74c`, наличие незакоммиченной, но
проверенной карточки 5.1 и сохранность посторонних untracked-файлов.

Обязательные локальные входы:

- `cities/cities1000.txt`;
- `cities/admin1CodesASCII.txt`;
- `cities/alternateNamesV2.txt`.

`cities/`, `data/` и generated SQLite остаются ignored и не добавляются в Git.

## Полная проверка кода

Последовательно выполнить:

```powershell
.\.venv\Scripts\python.exe -B -m pytest -p no:cacheprovider tests/test_place_search_contracts.py tests/test_place_catalog_builder.py tests/test_place_catalog_sqlite.py tests/test_place_catalog_search.py tests/application/test_place_catalog_integration.py tests/test_module_boundaries.py -q
.\.venv\Scripts\python.exe -B -m pytest -p no:cacheprovider tests/test_birth_places.py tests/test_birth_resolver.py tests/test_birth_tz.py tests/application -q
.\.venv\Scripts\python.exe -B -m pytest -p no:cacheprovider -q
```

Не объявлять ветку готовой только по targeted или related результату. Первый
полный отказ записать с точной командой/node IDs и границей влияния; не менять
тесты ради зелёного статуса.

## Full local-data build и smoke

Создать ignored `data/places.sqlite` настоящим CLI builder-ом из трёх полных
локальных дампов с default-параметрами. Не использовать сеть, synthetic
fixtures или альтернативные фильтры.

После успешной сборки зафиксировать в журнале:

- SHA-256 каждого исходного файла;
- полную JSON-статистику builder-а;
- точное `places_written` как якорь повторной сборки тех же входов;
- размер generated SQLite и metadata schema/tzdata version;
- фактический smoke через `SqlitePlaceCatalog`: `search("Москва")` содержит
  `524901`, а `lookup("524901")` возвращает `Москва`, `55.75/37.62` и
  `Europe/Moscow`.

Проверить, что `data/places.sqlite` игнорируется Git и отсутствует в status.

## Documentation closeout

Обновить только утверждения, статус которых изменён реализацией M1-5:

- основной place-catalog документ: M1-5 выполнен; contracts, builder,
  `SqlitePlaceCatalog`, search/lookup/lifecycle и acceptance реализованы;
- birth resolution: SQLite catalog и общий search/lookup lifecycle больше не
  являются остатком, при этом HTTP/UI recovery R-20 остаётся отложенным;
- overview/scenarios: реализованы port, outcomes, builder и leaf adapter;
  поисковый HTTP endpoint и UI по-прежнему target;
- sequence README и подписи существующих place-catalog diagrams: catalog core
  помечен implemented, transport/client остаются target;
- roadmap: M1-5 помечен выполненным 2026-09-22, остаток M1 уменьшен на четыре
  рабочих дня без удаления истории и без изменения оценок следующих этапов.

Не переписывать нормативную семантику, AC, SQL/ranking/lifecycle contracts или
архитектурные решения, если они уже совпадают с реализацией.

## Разрешённые файлы

- этот prompt-файл;
- `docs/project_management/implementation_plans/place_catalog_implementation_plan.md`;
- `docs/requirements/component_responsibilities/exact-orb_place_catalog.md`;
- `docs/requirements/component_responsibilities/exact-orb_birth_data_resolution.md`;
- `docs/requirements/overview.md`;
- `docs/requirements/scenarios.md`;
- `docs/sequence_diagrams/place_catalog/README.md`;
- только те `docs/sequence_diagrams/place_catalog/*.puml`, где меняется
  implemented/target status;
- `docs/project_management/roadmap.md`.

Generated `data/places.sqlite` разрешён только как ignored локальный артефакт,
не как изменение Git.

## Запреты

Не менять `src/**`, `tests/**`, builder, fixtures/goldens, ADR, соседние
requirements/diagrams, dependencies, raw GeoNames dumps и `.gitignore`.

Не добавлять HTTP/FastAPI, lifespan ownership, UI, debounce, rate limit,
RemoteGeocoder, сеть, background refresh, миграции каталога или deployment
packaging. Не завышать статус M1-6/M1-7/M1-12.

Не создавать commit, push или PR без отдельного указания владельца.

## Финальная приёмка документов

После правок выполнить:

- `git diff --check`;
- проверку всех относительных Markdown-ссылок изменённых документов;
- структурную проверку всех шести place-catalog PUML: одна пара
  `@startuml/@enduml`, уникальные aliases и сбалансированные `box/end box`;
- PlantUML render всех изменённых диаграмм, если Java и renderer доступны;
- `git status --short` и allowlist фактических изменений.

В журнале плана записать команды, реальные результаты, hashes/count anchor,
изменённые документы, недоступность renderer при её наличии и оставшиеся
границы M1-6/M1-7/M1-12. После этого карточки 1.1–5.2 и ветка M1-5 считаются
завершёнными; commit остаётся отдельным действием владельца.
